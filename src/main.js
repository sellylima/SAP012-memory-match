import App from "./components/App.js";

document.getElementById("root").appendChild(App()); //usa o metodo .appendChild para criar um filho do id "root" que está no html e chamar atraves da função App, criado no App.js

